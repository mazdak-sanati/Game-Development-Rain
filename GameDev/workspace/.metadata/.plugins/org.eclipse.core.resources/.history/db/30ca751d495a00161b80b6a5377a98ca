package com.thedude.rain.level;

public class RandomLevel extends Level{

	public RandomLevel(int width, int height) { //we used the int,int constructor inheriting from LEvel class, because we want to generate and not import a level
		super(width, height); //whatever the parameter we input here is going back to Level.Level(int,int) and run the constructor
		
	}
	
}
