package com.thedude.rain.graphics;

public class Screen {
	private int width, height;
	public int[] pixels;

	public Screen(int width, int height) {
		this.width = width;
		this.height = height;
		pixels = new int[width * height]; //pass in through the Game class
	}

	public void render() {
		for (int y = 0; y < height; y++) { //filling the entire screen through next loop
			for (int x = 0; x < width; x++) {
				pixels[40 + y * width] = 0x736744; //color in hex increasing pixels 1 by one and changing their color
			}
		}
	}
}
